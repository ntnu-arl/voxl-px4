# voxl-px4

High level PX4 wrapper project for CI and configuration with PX4 firmware as a submodule

## Getting started

This project contains the px4-firmware as a submodule, which itself contains nested submodules. After cloning this project get all of the submodules with the git command ```git submodule update --init --recursive```
