_voxl-configure-px4(){
	local OPTS=('help enable disable factory_enable sentinel fpv d0006 d0008')
	COMPREPLY=()
	if [ "$COMP_CWORD" -eq 1 ]; then
		COMPREPLY=( $(compgen -W '${OPTS}' -- ${COMP_WORDS[COMP_CWORD]}) )
		return 0
	fi
}

complete -F _voxl-configure-px4 voxl-configure-px4
